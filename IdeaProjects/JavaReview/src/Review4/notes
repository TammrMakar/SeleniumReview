Do while loop when we do not know how many times we need to repeat a block of code

break;//stops /break the loop
continue;//skips current iteration

//nested Loop- loop inside loop
//inner loop ALWAYS depends on outer loop
//outer loop controls number of executions for inner loop


   //for (int i = 1; i <=3; i++) {//outer & Rows  3ashan na3yed kam mara like 3 or 4 times
  // for (int j = 1; j <= 4; j++) {//inner & columns    3ashan na3yed el size elly gowah block code like 4 or 5